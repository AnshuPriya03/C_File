#include<stdio.h>
int main()
{
    int side,area;
    printf("enter the side of square\n",side);
    scanf("%d",&side);
    area=side*side;
    printf("the area of square is %d",area);
    
    return 0;

}